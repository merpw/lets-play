services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - /etc/ssl:/etc/ssl
  api:
    image: ghcr.io/merpw/lets-play:${REVISION-main}
    build: .
    environment:
      - MONGO_HOST=db
    depends_on:
      - db
  db:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - lets-play-db:/data/db
volumes:
  lets-play-db:

